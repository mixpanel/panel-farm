.animal-badge(class={visible: $helpers.isVisible()})
  img(attrs={
    src: `/images/${$component.getAttribute(`animal`)}.png`,
    height: 90,
    width: 90,
  })
  .close-button(on={click: $helpers.clickClose})
